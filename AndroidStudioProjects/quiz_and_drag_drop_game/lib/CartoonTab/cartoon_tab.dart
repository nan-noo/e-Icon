import 'package:flutter/material.dart';

class CartoonTab extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
        child: Icon(Icons.menu_book)
    );
  }
}

